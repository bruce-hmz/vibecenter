"""Vibe Center for Windows — notch panel for AI coding agents.

Cross-platform PySide6 port of the macOS VibeIsland.swift app. The
protocol layer (scanner output shape, IPC messages, HMAC auth) is
byte-compatible with the macOS app so relay.py and usage-daemon.py work
against either without changes.
"""

__version__ = "1.0.0"
